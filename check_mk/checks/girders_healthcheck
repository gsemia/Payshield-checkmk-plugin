# the inventory function (dummy)
def inventory_girders_healthcheck(info):
   print info
   return [] # return empty list: nothing found

# the check function (dummy)
def check_girders_healthcheck(item, params, info):
   return (3, "Sorry - not implemented")

# declare the check to Check_MK
check_info["girders.healthcheck"] = {
    'check_function':            check_girders_healthcheck,
    'inventory_function':        inventory_girders_healthcheck,
    'service_description':       'Girders Healthcheck %s',
}